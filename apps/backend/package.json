{
  "name": "mes-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch",
    "build": "nest build",
    "lint": "echo 'lint placeholder'",
    "test": "node --test test/engines.test.mjs"
  }
}
